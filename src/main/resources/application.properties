server.port=${SERVER_PORT:8084}
spring.application.name=fitness-workouts-api
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/ptfitness-workouts}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:Pawel123}
firebase.project-id=${FIREBASE_PROJECT_ID:ptprojectsweb}

# Fitness.Catalog.Api - the shared exercise catalogue this service searches and imports from.
catalog.api.url=${CATALOG_API_URL:http://localhost:8085/api}